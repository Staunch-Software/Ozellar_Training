/* Real course content, generated from the actual Ozellar Marine decks by
   scratchpad/build_courses.py (slides exported as images, text extracted,
   embedded videos pulled out). Regenerate that script to refresh. */
import data from './courses.json'

export const learner = data.learner
export const courses = data.courses
